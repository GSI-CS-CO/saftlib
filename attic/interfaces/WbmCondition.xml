<node xmlns:xi="http://www.w3.org/2001/XInclude" name="WbmCondition">
  <annotation name="de.gsi.saftlib.name" value="de.gsi.saftlib"/>
  
  <!-- Include base interfaces -->
  <xi:include href="Owned.xml"/>
  <xi:include href="Condition.xml"/>
  
  <!-- de.gsi.saftlib.WbmCondition:
       @short_description: Matched against incoming events on a WbmActionSink.
       
       WbmConditions are created by WbmActionSinks to select which
       events should generate callbacks. This interface always implies
       that the object also implements the general Condition interface.
    -->
  <interface name="de.gsi.saftlib.WbmCondition">
    <!-- Tag: The tag which is sent to the Wbm by this condition
      -->
    <!-- <property name="Tag" type="u" access="readwrite"/> -->
  </interface>
</node>
